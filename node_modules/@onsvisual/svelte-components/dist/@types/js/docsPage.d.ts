export namespace parameters {
    let viewMode: string;
    namespace previewTabs {
        namespace canvas {
            let hidden: boolean;
        }
    }
    let toolbar: {
        zoom: {
            hidden: boolean;
        };
        eject: {
            hidden: boolean;
        };
        fullscreen: {
            hidden: boolean;
        };
        "storybook/background": {
            hidden: boolean;
        };
        "storybook/outline": {
            hidden: boolean;
        };
    };
}
